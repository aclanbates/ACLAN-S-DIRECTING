rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /syncRooms/{syncCode} {
      allow read, write: if request.auth != null
        && syncCode.matches('^[A-Z0-9]{4}-[A-Z0-9]{4}-[A-Z0-9]{4}$');
    }
  }
}
